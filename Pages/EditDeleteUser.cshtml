@page
@model Neemah.Pages.EditDeleteUserModel
@{
    ViewData["Title"] = "Manage Users";
}

<div class="container mt-5">
    <h3 class="mb-4 text-center">👤 Manage Users</h3>

    @if (Model.Users != null && Model.Users.Any())
    {
        <table class="table table-bordered table-striped">
            <thead class="table-light">
                <tr>
                    <th>Username</th>
                    <th>User Type</th>
                    <th style="width: 200px;">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var user in Model.Users)
                {
                    <tr>
                        <form method="post">
                            <td>
                                <input type="hidden" name="Id" value="@user.Id" />
                                <input type="text" name="Username" value="@user.Username" class="form-control" />
                            </td>
                            <td>
                                <select name="UserType" class="form-select">
                                    <option value="Admin" selected="@(user.UserType == "Admin")">مشرف</option>
                                    <option value="User1" selected="@(user.UserType == "User1")">صاحب الفرع</option>
                                    <option value="User2" selected="@(user.UserType == "User2")">صاحب التتبيل</option>
                                </select>
                            </td>
                            <td class="d-flex gap-2">
                                <button type="submit" name="action" value="edit" class="btn btn-sm btn-success">Save</button>
                                <button type="submit" name="action" value="delete" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this user?');">Delete</button>
                            </td>
                        </form>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p class="text-muted text-center">No users found.</p>
    }
</div>
